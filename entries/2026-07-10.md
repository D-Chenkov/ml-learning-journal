# 2026-07-10 - MLflow Model Registry -> /predict integration (Week 3)

Intensive Week 3 day. Wired the MLflow Model Registry into the serving app: train -> track -> register -> serve is now a working loop (locally). Roadmap box #88 done (locally).

**Done (centerpiece):**
- Ran an MLflow tracking server; registered the iris model as `iris-clf` v1 and set the `@champion` alias.
- Changed /predict in k8s-fastapi-starter to load `models:/iris-clf@champion` from the registry instead of a baked joblib. Ran locally with MLFLOW_TRACKING_URI set; curl returns setosa.
- Key idea landed: promoting a new model = register a new version + move the alias, NO code change or redeploy. The alias is a stable symbolic pointer (like a DNS name), and the alias->version->artifact mapping is data in the registry, changeable independent of code.

**Reading:** Made With ML - MLOps course (foundations lessons). https://madewithml.com/courses/mlops/

**Daily problems:** Valid Parentheses (stack) - planned, not logged in detail.

**Flashcard round score:** ~7.2/10.
- Solid/closed: registry promotion (9), restart policies CLOSED (no / on-failure / always / unless-stopped), model-URI indirection.
- Improving: rolling update vs self-heal - recalled the mechanism (new RS up / old RS down; self-heal keeps the same RS), but still conflating ReplicaSet with replica/pod. Lock: 2 ReplicaSets for a rollout (old + new), 1 ReplicaSet for self-heal; "3" is the POD count each manages, not RS count.
- NEW mend: registry serving architecture. MLFLOW_TRACKING_URI = the ADDRESS of the registry server (WHERE), the alias = WHICH model. In a K8s pod, localhost does not reach my laptop, so I'd run MLflow as a Service, inject the URI via ConfigMap (K8s DNS like http://mlflow:5000), and use a shared artifact store (S3/GCS/MinIO). Code stays the same; infra changes.

**Next:** Drill 2-RS-vs-1-RS wording and the tracking-URI architecture. Continue Week 3: MLflow integrated inside K8s (mlflow as a service), CI/CD (GitHub Actions), and the Made With ML MLOps lessons.
