# 2026-07-14 - Project #1 portfolio-ready (k8s-fastapi-starter)

Milestone entry: took Project #1 (k8s-fastapi-starter) from "working" to "portfolio-ready." No new topic, no quiz - a consolidation/finishing pass during R1.

**Reviewed the repo for portfolio-readiness.** Git hygiene was already clean: venv/, __pycache__/, .pytest_cache/, *.pyc, and app/model.joblib are all gitignored, so only 16 real files are tracked (Dockerfile, app, all four k8s manifests, docker-compose, tests, CI, train_model, requirements). No junk committed.

**Gaps found and closed:**
- Stale README (the #1 gap). The old one still said "returns a version string" and listed only a Dockerfile + 2 manifests - it undersold everything built. Rewrote it fully: one-line summary + CI badge, an architecture diagram (train -> MLflow track/register -> FastAPI /predict loads by alias -> Docker -> K8s -> CI on push), what-it-demonstrates, accurate project tree, Kubernetes + local + Compose quickstarts, the MLflow registry alias-promotion section, a config table (MODEL_URI / MLFLOW_TRACKING_URI / APP_VERSION), Kubernetes details, tests/CI, and a roadmap. The README is what a recruiter reads first, so this was the highest-leverage fix.
- Added readiness + liveness probes on /health, and resource requests/limits, to k8s/deployment.yaml (production hygiene signals). Bumped the image tag so a fresh clone runs the build that has /predict.

**State of Project #1 - portfolio-ready:** a documented, CI-gated, registry-backed end-to-end ML serving pipeline. Train -> track/register in MLflow -> serve via FastAPI /predict (model pulled from the registry by @champion alias, promote with no code change) -> Docker (non-root) -> Kubernetes (Deployment/Service/ConfigMap/Secret, probes, resource limits, scaling, rolling updates) -> GitHub Actions CI (hermetic: generate model -> pytest gate -> build).

**Explicitly stretch (future, not gaps):** package as a Helm chart, deploy to GKE, and run MLflow as a Service in-cluster so pods load from the registry directly (the open architecture item).

**Reflection:** three weeks ago this repo returned a hardcoded string. It is now a real MLOps project I can hand someone. Good proof that the paced, one-piece-at-a-time approach compounds.

**Next:** one extra ML topic of my choosing (will report), then Week 4 / next phase.
