# 2026-08-05 - PyTorch Course 2, Module 4 (training performance) - Course 2 DONE

Finished Module 4, which completes Course 2 of the PyTorch for Deep Learning set. Module 4 is about making training fast and efficient: keeping the GPU fed and scaling the training loop. Only Course 3 (transformers, deployment) remains to finish the whole certificate.

**Topics covered (resources-driven):**
- DataLoader performance: keep the GPU from starving on input. Knobs are num_workers, pin_memory, persistent_workers, prefetching. Advanced: NVIDIA DALI (GPU-accelerated decode/augment), WebDataset tar shards for high-throughput streaming, threaded vs process-based loading trade-offs.
- Hardware for deep learning: GPU memory / bandwidth / compute trade-offs (Tim Dettmers guide), finding bottlenecks, and raising GPU utilization via data locality and caching.
- Batch size: the "critical batch size" where bigger batches stop helping; balancing gradient noise, speed, memory, and hardware efficiency.
- Prefetching + pinned memory: page-locked host memory (pin_memory=True) plus non-blocking transfers overlap host-to-device copies with compute so the GPU stays busy. Pinning helps when transfer-bound, wastes RAM otherwise.
- PyTorch Lightning: LightningModule / Trainer abstractions to remove training-loop boilerplate and improve reproducibility; callbacks, loggers, profiler.
- Mixed precision (AMP) + gradient accumulation: autocast + GradScaler speed up training with less memory at minimal stability cost; gradient accumulation simulates a larger batch by summing gradients over several steps before an optimizer step (mind schedulers and clipping).

**Takeaways to carry:** most of this is "don't let data loading or transfers bottleneck the GPU." The practical first-line knobs are num_workers, pin_memory, and AMP; gradient accumulation is the trick for fitting a large effective batch on limited memory. Relevant later for Project #3 training and any bigger model runs.

**Milestone:** Course 2 complete (Modules 1-4). Next is Course 3 to finish the certificate.

**Course 2 closeout quiz (Modules 2-4): 83/100 (8.3/10).** Big jump from Module 2 (66) and Module 3 (59). All four prior reds recovered to green:
- Freezing a layer: requires_grad = False (not no_grad, which is for inference; and model.eval() is a third thing that toggles Dropout/BatchNorm). 8/10.
- nn.Embedding vs nn.EmbeddingBag: lookup vs lookup+pooling; offsets avoid padding; also skips the big intermediate tensor. 9/10.
- Padding + attention mask: mask (1 real, 0 pad) zeroes pad in attention (-inf pre-softmax) and excludes it from pooling. 9/10.
- t-SNE caveat: cluster sizes / gaps / distances are not meaningful; local neighborhoods only; stochastic + perplexity-sensitive. 8/10.
New Module 4 ground, all solid: mixed precision / AMP (autocast picks per-op precision, GradScaler prevents fp16 gradient underflow) 8/10; gradient accumulation (step/zero_grad every N micro-batches, divide loss by N; saves memory not time) 9/10; pin_memory (page-locked host memory for async DMA transfer, pairs with non_blocking=True, helps only when transfer-bound) 8/10; tokenizer/weights same-checkpoint 9/10; transfer-learning two-step recipe 8/10.

Stubborn mend (slipped 3x): the augmentation EXCLUSION reason. One line to carry - augment train to generalize; keep val/test deterministic to measure honestly.

**Project #1 published (k8s-fastapi-starter).** Ran the Locust load test and closed the last portfolio gaps:
- Debugged a 100% /predict failure. Root cause chain: the running pods were an OLD image (fastapi-app:v4 from July 7) that predated the /predict route, so every call 404'd. Rebuilding + reloading did not take because k8s/deployment.yaml had an invalid apiVersion (apps/v6 instead of apps/v1), so kubectl apply silently rejected the Deployment and rollout restart just respun the old v4 pods. Fixed apiVersion to apps/v1, bumped image to v7, reloaded into minikube, re-applied. Then a clean run.
- Two lessons banked: a container serves its BUILT IMAGE, not live source; and apiVersion for a Deployment is always apps/v1 - never global-replace bare version strings or you clobber it.
- Load test result (100 concurrent users, minikube, 3 replicas via port-forward): POST /predict 13,804 requests, 0 failures, p50 350ms / p95 540ms / p99 660ms, ~115 req/s. Headline is zero failures under concurrency; the latency is the local port-forward tunnel, not the model (iris RF predicts in well under 1ms). Added an honest "Load testing" section + results table to the README, committed locustfile.py, gitignored the Locust CSVs.
- Remaining publish steps (click-throughs, no code): pin the repo on GitHub, add to LinkedIn Featured + skills.

**Next:** finish publishing Project #1 (pin + LinkedIn), then start PyTorch Course 3 (transformers, deployment) in Week 6.
