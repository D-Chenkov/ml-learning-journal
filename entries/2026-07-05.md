# 2026-07-05 - Light Sunday (code problem only)

Kept today light (late start). Just the daily code problem; the rest of Sunday's planned block moved to a long Monday.

**Daily problems (1):**
| # | Problem | Topics | Core technique |
|---|---------|--------|----------------|
| 1 | Longest Substring Without Repeating Characters | Sliding Window | One window [l, r]; grow r, and on a duplicate SHRINK from the left (remove s[l], l+=1) until the dup is gone, then add s[r]; track max size. Use a set (any char), not a 26-array (that crashed on spaces/digits/symbols). O(n): l and r only move forward, each char enters/leaves the set once. First sliding-window problem. |

**Deferred to Monday (2026-07-06):**
- Read: K8s Docs - ConfigMaps + Secrets (env vars + secrets securely).
- Practice: add ConfigMap + Secret + non-root container to k8s-fastapi-starter (also closes Project #1 roadmap items).
- Review: Docker vs K8s 1-page summary (Feynman) -> feeds a LinkedIn post (add Docker skill + project).
- Review: week-2 K8s quiz (run from this week's flashcard CSVs).

**Flashcard round (week review, ~4.6/10 avg - deliberately targeted unclosed mends, so the low score is diagnostic):**
Still open (drill Monday):
- Shadow release = outputs DISCARDED, no user sees them; canary = a fraction of users DO get the new model's outputs.
- Rolling update = TWO ReplicaSets (new scales up / old scales down, overlap = zero downtime). Self-healing = ONE ReplicaSet recreating a pod. "How many ReplicaSets?" tells you which.
- Selector != template labels -> Deployment REJECTED at apply time (validation error), not a silent "won't connect." (Routing/connect language belongs to Services.)
Closed/solid: delete-pod self-healing (one RS recreates to restore count); sliding-window O(n) reasoning (forward-only pointers).

**Next:** Long Monday - the deferred K8s block (ConfigMap/Secret read + practice in k8s-fastapi-starter, Docker-vs-K8s summary -> LinkedIn, week-2 quiz) plus drilling the three mends above.
