# 2026-07-01 - Rest day (light review round)

Break day. We skipped Tuesday's break, so took it today. No new material or coding problem, just a quick spaced-repetition round on still-open mend items. Rest matters, and spacing recall helps it stick.

**Flashcard round (quick, ~6.3/10 avg):**
| Topic | Score | Status |
|-------|-------|--------|
| Bucket-sort precondition (Top K Frequent) | 9 | Closed - had slipped twice on 06-24, now locked. Frequencies are bounded by n, so index into n+1 buckets instead of comparison-sorting -> O(n). |
| Shadow releasing | 5.5 | Needs mending - got the cost (double compute) and live-data benefit, but the core "outputs are NOT served to users" still didn't surface unprompted. Shadow discards outputs; canary serves them to a fraction of users. |
| Service -> pods | 3 | Priority mend - still conflating Service routing with pod scheduling. A Service picks pods by LABEL SELECTOR (and load-balances the matched set), NOT via the scheduler/resource calculation. |
| StatefulSet provides | 7.5 | Solid - named stable network identity + ordered ops. Third pillar to add: stable per-pod persistent storage. Caveat: it does not replicate data for you. |

**Still open (drill next session):**
- Service selects pods by label selector, not scheduling (priority).
- Shadow release = outputs discarded (vs canary = served to some users).

**Next:** Back to it tomorrow - finish the remaining ~2 hours of the Nana K8s video and run its flashcard round.
