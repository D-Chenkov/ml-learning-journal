# 2026-07-11 - Brief day: GitHub Actions review (quiz)

Short Saturday. Already had GitHub Actions basics down (gave a presentation on it before), so did a quiz to confirm and left the CI workflow build for tomorrow.

**Reviewed:** GitHub Actions fundamentals - workflows, triggers/events, jobs, steps, runners, secrets.

**Daily problems:** none (brief day).

**Flashcard round score:** ~7.9/10. Strong on GitHub Actions.
- Solid: workflow anatomy (Workflow -> Jobs -> Steps; runner = the VM a job runs on), jobs (parallel by default, serialize with needs:, isolated runners, pass files via artifacts; steps within a job share the filesystem), CI pipeline shape for the repo (checkout -> setup-python -> install -> test -> build).
- Firm-ups: checkout is a `uses: actions/checkout` action, NOT a run command; secrets referenced as ${{ secrets.NAME }} and are encrypted + auto-masked in logs (not just "don't hardcode").

**Still-open mends carried:** rolling update vs self-heal wording (2 ReplicaSets for a rollout, 1 for self-heal; "3" = pods); registry serving architecture (MLFLOW_TRACKING_URI = address of the registry; in a pod localhost won't reach the laptop, so MLflow-as-a-Service + ConfigMap URI + shared artifact store).

**Next (tomorrow):** build the CI workflow in k8s-fastapi-starter - add a minimal pytest (the test gate) + on-push build. Ties to Sunday's "GitHub Action: train + log on push." Then continue Week 3.
